pipeline {
 agent any
     stages {
       stage {'build'}
          steps {
             echo "hello"
           }
        stage {'Test'}
           steps {
            echo "Mukunth"
         }
   }
}
